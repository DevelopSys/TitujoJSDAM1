let frase = "Esto es un ejemplo de string";
console.log(frase);
for (let index = 0; index < frase.length; index++) {
  console.log(frase.charAt(index).toLowerCase());
}

// sacar por consola cada una de las letras que tiene la palabra
